package com.kg.template.servicename;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/*
 * Entry point of the spring boot application is the class containing
 * @SpringBootApplication annotation & the static main method
 */
@SpringBootApplication
public class ServiceNameApplication {

  public static void main(String[] args) {
    SpringApplication.run(ServiceNameApplication.class, args);
  }
}
